# SQLite workflow database. Container default: /data/workflows.db
WORKFLOW_DB_PATH=data/workflows.db

# Optional coarse bearer protection for /api/v1 endpoints. Do not commit a real value.
WORKFLOW_API_TOKEN=
